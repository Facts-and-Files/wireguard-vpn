# WG Easy VPN

## Starting the docker container

    $ docker compose up -d

Then stop the container:

    $ docker compose down

For a quick documentation see:
https://wg-easy.github.io/wg-easy/v15.1/

## Deployment

Basic deployment for setting up the docker coontainer is done by a Github workflow.
Client and server configuration is then done via provides Web UI.
